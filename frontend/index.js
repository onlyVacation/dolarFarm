import { registerRootComponent } from 'expo';
import App from './App';

// Registra o componente raiz da sua aplicação
// Este é o primeiro componente a ser renderizado quando o aplicativo inicia
registerRootComponent(App);
